# Models

Stage 19 removed the old full-house `home-library.glb` asset from the app.

The main bookshelf browser now renders the living-room composition in the app and keeps stable database scene keys for shelves/books. When a new `living_room.blend` source is exported later, place its browser-ready GLB here and wire it into the room view without changing shelf identifiers.
